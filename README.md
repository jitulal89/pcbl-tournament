PCBL V28

Fixes:
- Admin mobile More button uses a reliable JS popup instead of <details>.
- All Admin navigation links use explicit admin.html?page=... URLs.
- Admin navigation now performs real page navigation, so refresh preserves the selected Admin section.
- Public files/features remain unchanged from V27.
- No database changes required.
